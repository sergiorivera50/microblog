# microblog

## Usage

**Writing a new post (opens Obsidian)**

```bash
poetry run new
```

**Live reload server (opens default browser)**

```bash
poetry run serve
```

**Build site**

```bash
poetry run build
```